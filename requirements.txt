# Core API & task queue
fastapi>=0.115
uvicorn[standard]>=0.30
celery[redis]>=5.4
redis>=5.0
pydantic>=2.0
python-multipart>=0.0.9
aiofiles>=23.0
python-dotenv>=1.0

# Image processing
pillow>=10.0
numpy>=1.26
opencv-python>=4.9
scikit-learn>=1.4
scikit-image>=0.23
scipy>=1.12

# Colorimetry
colour-science>=0.4.4

# Wavelet smoothing (color_by_number contours)
PyWavelets>=1.6

# Skeleton graph (dot_to_dot)
sknw>=0.14

# PDF assembly
fpdf2>=2.7

# Geometry (edge contour JSON/SVG export)
shapely>=2.0

# Personal observations (Claude vision call, optional — degrades gracefully
# without ANTHROPIC_API_KEY)
anthropic>=0.40
